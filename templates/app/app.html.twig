{% extends 'base.html.twig' %}

{% block head %}
	<script src="/assets/js/app.js"></script>

{% endblock %}

{% block body %}


	<div class="row">
		<div class="col-md-6">
			<h2>Wybierz miasto</h2>
			<br>
			<select id="selectCity" class="form-control">
				{% for c in cities %}
					<option value="{{ c.id }}">{{ c.name }}</option>
				{% endfor %}
			</select>
			<br><br>
			<button class="btn btn-success btn-lg" id="refresh">Odświerz</button>
		</div>
		<div class="col-md-6">
			<div id="weatherDiv">
			
			</div>
		</div>
	</div>
	
	
	
	
	
{% endblock %}